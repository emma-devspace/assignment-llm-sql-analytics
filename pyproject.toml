[tool.black]
line-length = 120
target-version = ["py313"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.ruff.lint.per-file-ignores]
# sys.path manipulation before imports is intentional in scripts and tests
"scripts/*" = ["E402"]
"tests/*" = ["E402"]
